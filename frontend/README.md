# Frontend

Angular frontend for the IP Management Portal.

## Run locally

1. Install dependencies:
   `npm install`
1. Start the app:
   `npm start`
1. Open `http://localhost:4200`

## If using Azurite

1. Install Azurite to emulate azure storage
   `npm install azurite`
1. Run Azurite
   `azurite`
